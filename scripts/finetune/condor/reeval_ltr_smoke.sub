# Condor smoke test: 1 GPU, bd3lm bs1 arc_easy left_to_right (Exp1/Currency1 validation)
request_cpus   = 4
request_gpus   = 1
request_memory = 80G
request_disk   = 20G
requirements   = (CUDADeviceName == "NVIDIA A100-SXM4-80GB")

log_root = /fast/pmayilvahanan/condor_logs/reeval_ltr/smoke_$(ClusterId)
error    = $(log_root).err
output   = $(log_root).out
log      = $(log_root).log

MaxTime = 7200
periodic_remove = (JobStatus =?= 2) && ((CurrentTime - JobCurrentStartDate) >= $(MaxTime))

executable = /lustre/fast/fast/pmayilvahanan/Interplay-LM-Reasoning/scripts/finetune/condor/reeval_ltr_smoke.sh
getenv = True

queue 1
