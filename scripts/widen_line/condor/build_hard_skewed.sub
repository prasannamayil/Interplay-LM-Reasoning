# CPU job: build the hard-skewed GSM lingua source (op8-10 oversampled). No GPU.
request_cpus   = 4
request_memory = 16G
request_disk   = 40G
log_root = /fast/pmayilvahanan/condor_logs/widen/hardskew_$(ClusterId)
error    = $(log_root).err
output   = $(log_root).out
log      = $(log_root).log
MaxTime = 14400
periodic_remove = (JobStatus =?= 2) && ((CurrentTime - JobCurrentStartDate) >= $(MaxTime))
executable = /lustre/fast/fast/pmayilvahanan/Interplay-LM-Reasoning/scripts/widen_line/build_hard_skewed.sh
getenv = True
queue 1
