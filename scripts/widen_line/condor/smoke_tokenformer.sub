# Tokenformer-only smoke (isolate the hang).
request_cpus   = 8
request_gpus   = 1
request_memory = 96G
request_disk   = 40G
requirements   = (CUDADeviceName == "NVIDIA A100-SXM4-80GB")
log_root = /fast/pmayilvahanan/condor_logs/widen/smoke_tf_$(ClusterId)
error    = $(log_root).err
output   = $(log_root).out
log      = $(log_root).log
MaxTime = 2400
periodic_remove = (JobStatus =?= 2) && ((CurrentTime - JobCurrentStartDate) >= $(MaxTime))
environment = "ARCHS=tokenformer"
executable = /lustre/fast/fast/pmayilvahanan/Interplay-LM-Reasoning/scripts/widen_line/smoke_all.sh
getenv = True
queue 1
